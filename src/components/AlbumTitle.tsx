import styled from 'styled-components';

const AlbumTitle = styled.h3`
  font-size: ${(props) => props.theme.fontSizes.title03};
  line-height: 30px;
  padding: 54px 0 44px 0;
`;

export default AlbumTitle;
