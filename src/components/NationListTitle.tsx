import styled from 'styled-components';

const NationListTitle = styled.h4`
  font-size: ${(props) => props.theme.fontSizes.title04};
  line-height: 20px;
  padding: 24px 0 32px 0;
`;

export default NationListTitle;
