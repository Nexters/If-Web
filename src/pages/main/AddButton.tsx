import React, { FC } from 'react';
import { Link } from 'react-router-dom';

const AddButton: FC = () => {
  return (
    <Link to="/add">
      button
      {/* button img */}
    </Link>
  );
};

export default AddButton;
